name: Go Vulnerability Check

on:
  workflow_call:
    inputs:
      container_image:
        required: true
        type: string

env:
  CGO_LDFLAGS_ALLOW: "-s|-w"
  PKG_CONFIG_LIBDIR: /opt/imgproxy/lib/pkgconfig
  LD_LIBRARY_PATH: /opt/imgproxy/lib
  GOFLAGS: -buildvcs=false
  # The job already runs inside inputs.container_image (the pinned base
  # image) via the "container:" key below, so tell guard_docker (in .runrc)
  # not to try to docker-in-docker its way into another copy of it.
  IMGPROXY_IN_BASE_CONTAINER: "1"

jobs:
  govulncheck:
    runs-on: ubuntu-latest
    container:
      image: ${{ inputs.container_image }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup cache
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Download mods
        run: go mod download

      - name: Mark git workspace as safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Run govulncheck (symbol scan)
        run: ./run govulncheck
